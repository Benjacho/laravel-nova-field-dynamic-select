{
    "name": "esferamedica/laravel-nova-field-dynamic-select",
    "description": "A Laravel Nova field.",
    "keywords": [
        "laravel",
        "nova"
    ],
    "license": "MIT",
    "require": {
        "php": ">=7.1.0"
    },
    "autoload": {
        "psr-4": {
            "EsferaMedica\\DynamicSelect\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "EsferaMedica\\DynamicSelect\\FieldServiceProvider"
            ]
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
